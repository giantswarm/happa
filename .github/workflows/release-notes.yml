name: Release Notes Creator
on:
  push:
    branches:
     - add-release-notes-creator
jobs:
  create-release-notes:
    runs-on: ubuntu-latest
    steps:
    - uses: release-drafter/release-drafter@v5
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
