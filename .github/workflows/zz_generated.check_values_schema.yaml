# DO NOT EDIT. Generated with:
#
#    devctl
#
#    https://github.com/giantswarm/devctl/blob/86ed4fd59b4841fef6e890cbacde888726ca39c3/pkg/gen/input/workflows/internal/file/check_values_schema.yaml.template
#

name: check-values-schema

on:
  pull_request:
    branches:
      - master
      - main
    paths:
      - 'helm/**/values.yaml'         # default helm chart values
      - 'helm/**/values.schema.json'  # schema
      - 'helm/**/ci/ci-values.yaml'   # overrides for CI (can contain required entries)

permissions: {}

jobs:
  check-values-schema:
    uses: giantswarm/github-workflows/.github/workflows/chart-values.yaml@main
    permissions:
      contents: read
