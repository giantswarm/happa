apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.name }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: nginx
  rules:
    {{- $serviceName := .Values.name -}}
    {{- $port := .Values.port -}}
    {{- if or (not (.Values.ingress.hostnames)) (eq (len .Values.ingress.hostnames) 0) }}
      {{- fail "value for .Values.ingress.hostnames must not be empty" }}
    {{- end }}
    {{- range .Values.ingress.hostnames }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $serviceName }}
                port:
                  number: {{ $port }}
    {{- end }}
  tls:
    - hosts:
      {{- range .Values.ingress.hostnames }}
      - {{ . }}
      {{- end }}
      secretName: {{ .Values.name }}
