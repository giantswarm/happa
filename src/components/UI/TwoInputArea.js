import styled from '@emotion/styled';

const TwoInputArea = styled.div`
  display: flex;

  div {
    flex: 1;
    margin-right: 25px;

    &:last-child {
      margin-right: 0px;
    }
  }
`;

export default TwoInputArea;
