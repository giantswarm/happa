import { Meta } from '@storybook/react';

import FlashMessagesProvider from '../FlashMessagesProvider';

export { Configurable } from './Configurable';

export default {
  title: 'Util/FlashMessages',
  component: FlashMessagesProvider,
} as Meta;
